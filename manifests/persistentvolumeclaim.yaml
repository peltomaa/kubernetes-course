apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kube-course-claim
spec:
  storageClassName: kube-course-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
