apiVersion: v1
kind: Service
metadata:
  name: crud-backend-svc
  namespace: crud
spec:
  type: NodePort
  selector:
    app: crud-backend
  ports:
    - port: 2348
      protocol: TCP
      targetPort: 3000
